import { ErrorCharactersNotFoundInfo } from './CharactersNotFound.styled';

const CharactersNotFound = () => {
  return (
    <ErrorCharactersNotFoundInfo>
      I cant find any of characters with this name. Try another name please.
    </ErrorCharactersNotFoundInfo>
  );
};

export default CharactersNotFound;